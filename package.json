{
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "tailwind": "npx tailwindcss -i ./assets/tailwind/input.css -o ./assets/css/output.css --watch",
    "build": "tailwindcss build ./styles.css -o output.css"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.1"
  }
}
